/*! \file height.h
    \brief A documented header file
    
    Contains declaration for height()
*/
/*! \fn  int height(node * temp)
    \brief Returns height of binary tree with root at \a temp
    \param temp The root node for binary tree.
*/
int height(node * temp);