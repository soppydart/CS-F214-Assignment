/*! \file draw.h
    \brief A documented header file
    
    Contains declaration for draw()
*/
/*! \fn  int draw(node * temp, int height)
    \brief Draws on terminal, a binary rooted parse tree, with root at \a temp , and height \a height.
    \param temp The root node for binary tree.
    \param height  Height of the parse tree.
*/
int draw(node * temp, int height);